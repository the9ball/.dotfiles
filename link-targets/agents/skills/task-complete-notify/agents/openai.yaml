interface:
  display_name: "Task-complete notification"
  short_description: "Arm or cancel a one-shot completion notification"
  default_prompt: "Use $task-complete-notify only after the user explicitly asks to arm or cancel a completion notification."
