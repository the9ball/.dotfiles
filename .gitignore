*.local
*.log

# 移行期間中の旧ツリーに導入した第三者スキル。旧実体は追跡せず、
# 出所は旧 `.agents/.skill-lock.json` に記録する。
# 復元手順は README.manual.md を参照する。
.agents/skills/gh-stack/

# 移行後の第三者スキル。実体は追跡せず、
# 出所は link-targets/agents/.skill-lock.json に記録する。
# 復元手順は README.manual.md を参照する。
link-targets/agents/skills/gh-stack/

# npx skills add で導入した第三者スキル。実体は追跡せず、
# 出所はルートの skills-lock.json に記録する。
# 復元手順は README.manual.md を参照する。
.agents/skills/dd-docs/
.agents/skills/dd-logs/
.agents/skills/dd-pup/
link-targets/agents/skills/dd-docs/
link-targets/agents/skills/dd-logs/
link-targets/agents/skills/dd-pup/

# 個人用の private CLI 由来スキル。実体は追跡せず、各自の環境で用意する。
.agents/skills/backlog-cli/
.agents/skills/redmine-cli/
link-targets/agents/skills/backlog-cli/
link-targets/agents/skills/redmine-cli/
